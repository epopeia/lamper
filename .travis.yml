language: python
python:
  - "3.6"
install:
  - pip install pytest
  - pip install pytest-cov
  - pip install coveralls
  - python setup.py install
  - python setup.py test
script:
  - pytest --cov=lamper/
after_success:
  - coveralls