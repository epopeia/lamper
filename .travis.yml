language: python
python:
  - "3.6"
install:
  - pip install pytest
  - pip install pytest-cov
  - pip install coveralls
  - python setup.py build
script:
  - pytest --cov=lamper/
after_success:
  - coveralls